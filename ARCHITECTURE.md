## סשן: תכנון ובניית פרוטוטייפ - Personal Life Coach

### קונספט
אפליקציית AI אישי שבונה למשתמש תוכנית יומית מותאמת אישית, לומדת ממנו לאורך זמן (מה עובד/לא עובד), ומתעדכנת כל יום מחדש על בסיס דיווח ערב קצר. פילוסופיה: "לא צריך להיות מושלם, צריך להיות עקבי". שם עבודה נוכחי לאפליקציה: **"גרסה 2"** (עדיין לא סופי).

### זרימת משתמש (MVP)
1. בחירת שפה (עברית/אנגלית) - מסך דו-לשוני כדי שיהיה מובן גם לפני שנבחרה שפה
2. התחברות עם אימייל (magic link, בלי סיסמה)
3. בחירת תחומי עניין (כושר, שינה, לימודים, קריירה, כסף, אנגלית, ספורט, הרגלים, ניהול זמן) - בחירה מרובה
4. בחירת תחום להתחלה ראשונה
5. שעת check-in יומי
6. שאלות עומק: זמן פנוי ביום, מה גורם לוותר, מידע נוסף חופשי
7. טוגל אופציונלי: מעקב משקל (מפעיל מעקב קלוריות)
8. AI בונה תוכנית יום 1 (2-4 משימות + הערת מאמן)
9. מסך "היום": רשימת משימות לסימון, אופציונלי מעקב קלוריות עם סריקת תמונת אוכל (vision)
10. דיווח ערב: מה עשית / מה קשה / על מה להתמקד מחר
11. AI בונה תוכנית ליום הבא בהתבסס על ההיסטוריה (5 ימים אחרונים)

### ארכיטקטורה טכנית
- **פרונטאנד**: HTML/CSS/JS וניל (קובץ יחיד), בלי framework - נבחר בכוונה בשביל הפשטות וללמידה
- **אימות**: Supabase Auth (email magic link)
- **מסד נתונים**: Supabase Postgres, טבלה יחידה `app_state` (user_id, state jsonb) עם RLS - כל משתמש רואה רק את הנתונים שלו
- **AI**: Supabase Edge Function בשם `ask-coach` (Deno) שמפעילה כ-proxy מול Anthropic API (`claude-sonnet-4-6`), שומרת את מפתח ה-API כ-secret בצד השרת - המשתמשים לא נחשפים למפתח בכלל
- **PWA**: manifest.json + sw.js + 3 אייקונים (מדליית זהב, בעיצוב תואם לפלטת האפליקציה) - מאפשר "הוספה למסך הבית"
- **אחסון סטטי**: מיועד ל-Netlify/Vercel (עוד לא הועלה בפועל)

### החלטות שהתקבלו לאורך הדרך
- הוסר פיצ'ר מעקב צעדים - אתר/PWA לא יכול לגשת למד-הצעדים המובנה של הטלפון (הרשאה ששמורה לאפליקציות טבעיות בלבד)
- מעקב קלוריות ממומש באמת: תמונת ארוחה -> Claude vision -> הערכת קלוריות + תיאור, ממלא אוטומטית
- ה-AI נותן "ידע בסיסי אך יעיל" בכל תחום, לא עומק מקצועי מקסימלי - כי לכל משתמש תחומי עניין שונים
- החלטה על ארכיטקטורת אימות עצמאית (Supabase) במקום להסתמך על גישת ה-API האוטומטית שזמינה רק בתוך שיחת Claude

### קבצים קיימים (נוצרו בצ'אט, טרם הועברו ל-repo)
personal-life-coach.html, manifest.json, sw.js, icon-192.png, icon-512.png, apple-touch-icon.png, setup.sql (יצירת טבלת app_state + RLS), ask-coach.ts (Edge Function)

### עוד לא בוצע
- יצירת פרויקט Supabase בפועל
- פרסום ה-Edge Function
- יצירת מפתח Anthropic API ושמירתו כ-secret
- העלאה ל-Netlify/Vercel
- סנכרון הקבצים לתוך ה-repo של ayalsite (WSL)

### הסדר עבודה עם Claude Code
מהשיחה הזו הלאה: Claude (בצ'אט) = ארכיטקט/מתכנן, כותב פרומפטים. Claude Code (ב-VS Code, בתוך WSL/Ubuntu) = המתכנת שמבצע בפועל. איל לומד תכנות מאפס, בלי ניסיון בטרמינל - כל שלב מוסבר לפני שמריצים אותו, צעד אחד בכל פעם.